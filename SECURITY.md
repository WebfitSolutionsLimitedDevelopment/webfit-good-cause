# Security

Do not report security vulnerabilities through public campaign forms. Use the operator's private security contact once published.

Production requirements include administrator MFA, least-privilege Supabase policies, private identity-document storage, signed URLs, encrypted transport, verified Stripe webhooks, rate limiting, audit logs, dependency updates, secrets management through Vercel, database backups and an incident-response process.
