import Link from 'next/link';import { requireStaff } from '@/lib/auth';import { createServiceClient } from '@/lib/supabase-server';import { AdminNav } from '@/components/admin/AdminNav';
export default async function Admin(){const {profile,role}=await requireStaff();const s=createServiceClient();const [{count:campaigns},{count:reviews},{count:reports},{count:payouts}]=await Promise.all([s.from('campaigns').select('*',{count:'exact',head:true}),s.from('campaigns').select('*',{count:'exact',head:true}).in('status',['submitted','under_review','enhanced_review','more_information_required']),s.from('reports').select('*',{count:'exact',head:true}).eq('status','open'),s.from('payouts').select('*',{count:'exact',head:true}).in('status',['pending','held','under_review'])]);return <section className="section"><div className="shell"><div className="admin-head"><div><div className="eyebrow">Good Cause administration</div><h1>Operations dashboard</h1><p className="muted">Signed in as {profile?.full_name||profile?.email} · {role.replaceAll('_',' ')}</p></div><form action="/auth/signout" method="post"><button className="button secondary">Log out</button></form></div><AdminNav/><div className="admin-grid"><div className="card metric"><span>All campaigns</span><strong>{campaigns||0}</strong></div><div className="card metric"><span>Review queue</span><strong>{reviews||0}</strong></div><div className="card metric"><span>Open reports</span><strong>{reports||0}</strong></div><div className="card metric"><span>Payout attention</span><strong>{payouts||0}</strong></div></div><div className="card manage-card"><h2>Master controls</h2><p>Campaign changes, compliance decisions, role changes and payout actions are recorded in the audit log.</p><Link className="button" href="/admin/campaigns">Manage all campaigns</Link></div></div></section>}
