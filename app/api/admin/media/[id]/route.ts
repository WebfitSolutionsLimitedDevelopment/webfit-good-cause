import { NextResponse } from 'next/server';import { requireStaff } from '@/lib/auth';import { createServiceClient } from '@/lib/supabase-server';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){await requireStaff(['reviewer','admin','super_admin']);const {id}=await params;const db=createServiceClient();const {data:item}=await db.from('campaign_media').select('storage_path,kind,url').eq('id',id).maybeSingle();if(!item)return NextResponse.json({error:'Not found'},{status:404});if(item.url)return NextResponse.redirect(item.url);if(!item.storage_path)return NextResponse.json({error:'Media unavailable'},{status:404});const {data,error}=await db.storage.from(process.env.SUPABASE_CAMPAIGN_MEDIA_BUCKET||'campaign-media').createSignedUrl(item.storage_path,300);if(error||!data?.signedUrl)return NextResponse.json({error:'Media unavailable'},{status:404});return NextResponse.redirect(data.signedUrl)}
