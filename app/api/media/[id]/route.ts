import { NextResponse } from 'next/server';import { createServiceClient } from '@/lib/supabase-server';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const db=createServiceClient();const {data:item}=await db.from('campaign_media').select('storage_path,status,kind').eq('id',id).maybeSingle();if(!item||item.status!=='approved'||item.kind!=='image'||!item.storage_path)return NextResponse.json({error:'Not found'},{status:404});const {data,error}=await db.storage.from(process.env.SUPABASE_CAMPAIGN_MEDIA_BUCKET||'campaign-media').createSignedUrl(item.storage_path,300);if(error||!data?.signedUrl)return NextResponse.json({error:'Media unavailable'},{status:404});return NextResponse.redirect(data.signedUrl)}
