import { CampaignCard } from '@/components/CampaignCard';import { getPublicCampaigns } from '@/lib/public-campaigns';
export default async function Campaigns(){const campaigns=await getPublicCampaigns();return <><section className="page-hero"><div className="shell"><div className="eyebrow">Explore Good Cause</div><h1>Find a cause to support</h1><p className="muted">Every published campaign completes Good Cause review before accepting donations.</p></div></section><section className="section"><div className="shell">{campaigns.length?<div className="grid-3">{campaigns.map(c=><CampaignCard key={c.slug} campaign={c}/>)}</div>:<div className="card empty-state"><h2>No live campaigns right now</h2><p>Approved campaigns will appear here as soon as they go live.</p></div>}</div></section></>}
