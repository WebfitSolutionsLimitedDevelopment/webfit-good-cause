import Link from 'next/link';import { PublicCampaign } from '@/lib/public-campaigns';import { money } from '@/lib/fees';
export function CampaignCard({campaign}:{campaign:PublicCampaign}){const pct=campaign.goal>0?Math.min(100,Math.round(campaign.raised/campaign.goal*100)):0;return <article className="campaign-card polished-card"><div className="campaign-visual campaign-photo">{campaign.slug==='nepal-flash-flood-relief-2026'?<img src="/campaigns/nepal-flash-flood-relief-2026.png" alt="Flood damage in Nepal"/>:<div className="nepal-visual visual-fill" aria-hidden="true"><div className="mountain mountain-one"></div><div className="mountain mountain-two"></div><div className="river"></div></div>}<div className="campaign-chip">{campaign.category}</div></div><div className="card-body"><div className="eyebrow">{campaign.location}</div><h3><Link href={`/campaigns/${campaign.slug}`}>{campaign.title}</Link></h3><p>{campaign.summary}</p><div className="progress"><i style={{width:`${pct}%`}}/></div><div className="campaign-stats"><strong>{money(campaign.raised)}</strong><span>{campaign.goal>0?`raised of ${money(campaign.goal)}`:'raised'}</span></div><div className="card-meta"><span>{campaign.donors} supporters</span><span>Good Cause reviewed</span></div><Link className="text-cta" href={`/campaigns/${campaign.slug}`}>View cause and donate</Link></div></article>}
