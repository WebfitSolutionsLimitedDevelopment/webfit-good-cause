'use client';
import { FormEvent,useState } from 'react';
export function DocumentUploader({campaignId}:{campaignId:string}){const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const data=new FormData(e.currentTarget);data.set('campaignId',campaignId);const r=await fetch('/api/documents/upload',{method:'POST',body:data});const j=await r.json();setMsg(j.error||'Document uploaded for review.');if(r.ok)e.currentTarget.reset();setBusy(false);}return <form className="card manage-card" onSubmit={submit}><h3>Upload verification evidence</h3><label>Document type<select name="kind"><option value="identity">Identity</option><option value="address">Address</option><option value="beneficiary_consent">Beneficiary consent</option><option value="purpose_evidence">Purpose evidence</option><option value="bank_evidence">Payment destination evidence</option><option value="organisation">Organisation records</option><option value="other">Other</option></select></label><label>File<input type="file" name="file" accept=".pdf,.jpg,.jpeg,.png,.webp" required/></label><button className="button" disabled={busy}>{busy?'Uploading...':'Upload securely'}</button>{msg&&<p className="muted">{msg}</p>}</form>}
